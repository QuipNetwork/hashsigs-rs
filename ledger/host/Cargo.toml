[package]
name = "hashsigs-ledger-vectors"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
license = "AGPL-3.0-or-later"
publish = false

[workspace]

[dependencies]
hashsigs-rs = { path = "../..", default-features = false, features = ["std", "profile-256s-sha2", "heap-buffers"] }
hex = "=0.4.3"
serde_json = "=1.0.151"
